<%= form_with(model: recomendation, local: true) do |form| %>
  <% if recomendation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recomendation.errors.count, "error") %> prohibited this recomendation from being saved:</h2>

      <ul>
      <% recomendation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
