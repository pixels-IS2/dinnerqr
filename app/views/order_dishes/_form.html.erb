<%= form_with(model: order_dish, local: true) do |form| %>
  <% if order_dish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_dish.errors.count, "error") %> prohibited this order_dish from being saved:</h2>

      <ul>
      <% order_dish.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
