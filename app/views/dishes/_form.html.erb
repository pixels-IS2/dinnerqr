<%= form_with(model: dish, local: true) do |form| %>
  <% if dish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dish.errors.count, "error") %> prohibited this dish from being saved:</h2>

      <ul>
      <% dish.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :dish_name %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.select(:state, ['Disponible','Agotada','Descuentp']) %>
  </div>

    <div class="field">
      portada:<%= form.file_field :photo %>


    </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :dish_description %>
  </div>

  <div class="field">
    <%= form.label :speciality %>
    <%= form.text_field :speciality, id: :dish_speciality %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :dish_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
