<%= form_with(model: table, local: true) do |form| %>
  <% if table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(table.errors.count, "error") %> prohibited this table from being saved:</h2>

      <ul>
      <% table.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number, id: :table_number %>
  </div>

  <div class="field">
    <%= form.label :sector %>
    <%= form.text_field :sector, id: :table_sector %>
  </div>

  <div class="field">
    <%= form.label :waiter %>
    <%= form.select :waiter_id, options_for_select(@waiters.map{|w|[w.email, w.id]}, params[:wailter_id]) %>

  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
